export const API_BASE = `http://${window.location.hostname}:9999/api`;
export const BASE_URL = `http://${window.location.hostname}:9999`;
export const FRONTEND_BASE = `http://${window.location.hostname}:48317`;
